alias: Gem's phone location
sequence:
  - data:
      where: gem
      message: |
        Your phone is in the {{ states('sensor.gem_location')}}
    action: script.jarvis_speech_engine_v3_0
description: ""
